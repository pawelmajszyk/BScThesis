spring:
  application:
    name: Users
  config:
    import: optional:configserver:http://${ConfigService:localhost}:8888
  datasource:
    driver-class-name: org.postgresql.Driver
    schemaName: ${spring.application.name}
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DATABASE:Cinema}?currentSchema=users&stringtype=unspecified&ApplicationName=${spring.application.name}&reWriteBatchedInserts=true
    username: ${DBUSER:cinema}
    password: ${DBPASSWORD:cinema}
    # if true initializes db using sql scripts
    initialize: false
  jpa:
    hibernate:
      ddl-auto: create-drop
      hbm2dll:
        create_namespaces: true
        default_schema: adapterborder
    properties:
      javax:
        persistence:
          schema-generation:
            database:
              action: create
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:28080/auth/realms/cinema/protocol/openid-connect/certs
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8887/eureka}
  instance:
    preferIpAddress: true


